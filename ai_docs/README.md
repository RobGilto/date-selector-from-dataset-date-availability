# AI Docs

Reference material AI agents (Claude, GLM via pi) should read before planning/building.

Add files here:
- `ryuu_v6_api.md` — ryuu.js v6 surface (domo.get, requestVariablesUpdate, onDataUpdated, onVariablesUpdated)
- `app_studio_variables.md` — how App Studio variables / functionIds work
- `domo_custom_app_lifecycle.md` — dev/publish via npx ryuu
- `manifest_format.md` — public/manifest.json fields, design id sync

For now, the sibling `domo-ai-vibe-rules/` directory is the authoritative source — see its README.
